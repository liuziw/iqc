<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iqc.system.mapper.SysOrganizationMapper">

    <!--
    /**
     * 代码自动生成，请不要修改，切记！！！
     */
    -->

    <sql id="selectAllPropertiesAsDO">
            select  id id, name name, description description, pid pid, path path, is_deleted isDeleted, gmt_create gmtCreate, gmt_modified gmtModified from sys_organization
    </sql>

    <sql id="selectByFieldNamesAsDO">
       select <foreach collection="fieldNames" index="index" item="item"  separator=",">${item}</foreach> from sys_organization
    </sql>

    <sql id="selectAsDO">
        <choose>
            <when test="fieldNames==null or fieldNames.length==0">
                  <include refid="selectAllPropertiesAsDO"/>
            </when>
            <otherwise>
                  <include refid="selectByFieldNamesAsDO"/>
            </otherwise>
        </choose>
    </sql>


    <select id="listAll" resultType="com.iqc.system.dbo.SysOrganizationDO">
        <include refid="selectAsDO" /> where is_deleted=0
    </select>

    <select id="mapAll" resultType="com.iqc.system.dbo.SysOrganizationDO">
        <include refid="selectAsDO" /> where is_deleted=0
    </select>


    <select id="getById" resultType="com.iqc.system.dbo.SysOrganizationDO">
        <include refid="selectAsDO" /> where id=#{id} and is_deleted=0
    </select>


    <select id="listByIds" resultType="com.iqc.system.dbo.SysOrganizationDO">
        <include refid="selectAsDO" /> where is_deleted=0 and id in (<foreach collection="ids" index="index" item="item"  separator=",">#{item}</foreach>)
    </select>

    <select id="mapByIds" resultType="com.iqc.system.dbo.SysOrganizationDO">
        <include refid="selectAsDO" /> where is_deleted=0 and id in (<foreach collection="ids" index="index" item="item"  separator=",">#{item}</foreach>)
    </select>

    <update id="deleteById">
        update  sys_organization set is_deleted=1,gmt_modified=CURRENT_TIMESTAMP where id=#{id}
    </update>

    <update id="deleteByIds">
        update  sys_organization set is_deleted=1,gmt_modified=CURRENT_TIMESTAMP where id in (<foreach collection="ids" index="index" item="item"  separator=",">#{item}</foreach>)
    </update>


    <update id="updateNotNullProperties">
        update sys_organization set
                <if test="id != null">id=#{id},</if>
                <if test="name != null">name=#{name},</if>
                <if test="description != null">description=#{description},</if>
                <if test="pid != null">pid=#{pid},</if>
                <if test="path != null">path=#{path},</if>
                gmt_modified=CURRENT_TIMESTAMP
        where id=#{id}
    </update>

    <update id="updateAllProperties">
        update sys_organization set
                id=#{id},
                name=#{name},
                description=#{description},
                pid=#{pid},
                path=#{path},
                gmt_modified=CURRENT_TIMESTAMP
        where id=#{id}
    </update>


    <insert id="insertAllProperties">
              <selectKey keyProperty="id" resultType="java.lang.Long" order="AFTER">
                        SELECT LAST_INSERT_ID() as id
                      </selectKey>

        insert into sys_organization
        (
                id,
                name,
                description,
                pid,
                path,
                is_deleted,gmt_create,gmt_modified
        )
        values
        (
                #{id},
                #{name},
                #{description},
                #{pid},
                #{path},
                0,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP
        )
    </insert>


    <insert id="insertNotNullProperties">
                      <selectKey keyProperty="id" resultType="java.lang.Long" order="AFTER">
                                SELECT LAST_INSERT_ID() as id
                              </selectKey>
        insert into sys_organization
        (
                <if test="id!=null">id,</if>
                <if test="name!=null">name,</if>
                <if test="description!=null">description,</if>
                <if test="pid!=null">pid,</if>
                <if test="path!=null">path,</if>
                is_deleted,gmt_create,gmt_modified
        )
        values
        (
                <if test="id!=null">#{id},</if>
                <if test="name!=null">#{name},</if>
                <if test="description!=null">#{description},</if>
                <if test="pid!=null">#{pid},</if>
                <if test="path!=null">#{path},</if>
                0,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP
        )
    </insert>

        <insert id="insertAllPropertiesList">
           insert into sys_organization
           (
                   id,
                   name,
                   description,
                   pid,
                   path,
                   is_deleted,gmt_create,gmt_modified
                   )
           values
           <foreach collection="list" item="item" index="index" separator="," >
           (
                   #{item.id},
                   #{item.name},
                   #{item.description},
                   #{item.pid},
                   #{item.path},
                   0,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP
           )
           </foreach>
       </insert>



    <select id="listByProperty" resultType="com.iqc.system.dbo.SysOrganizationDO">
        <include refid="selectAsDO" />
            where is_deleted=0 and ${propertyName}=#{value}
    </select>

    <select id="countByProperty" resultType="java.lang.Integer">
        select count(1) from sys_organization
            where is_deleted=0 and ${propertyName}=#{value}
    </select>

    <select id="mapByProperty" resultType="com.iqc.system.dbo.SysOrganizationDO">
        <include refid="selectAsDO" />
            where is_deleted=0 and ${propertyName}=#{value}
    </select>


    <update id="updateNotNullFieldsByProperty">
        update sys_organization set
                <if test="updateDO.id != null">id=#{updateDO.id},</if>
                <if test="updateDO.name != null">name=#{updateDO.name},</if>
                <if test="updateDO.description != null">description=#{updateDO.description},</if>
                <if test="updateDO.pid != null">pid=#{updateDO.pid},</if>
                <if test="updateDO.path != null">path=#{updateDO.path},</if>
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0 and ${propertyName}=#{value}
    </update>

    <update id="updateAllFieldsByProperty">
        update sys_organization set
                id=#{updateDO.id},
                name=#{updateDO.name},
                description=#{updateDO.description},
                pid=#{updateDO.pid},
                path=#{updateDO.path},
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0 and ${propertyName}=#{value}
    </update>

    <update id="deleteByProperty">
        update sys_organization set
                is_delete=1,
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0 and ${propertyName}=#{value}
    </update>

    <select id="listByProperties" resultType="com.iqc.system.dbo.SysOrganizationDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="propertyNames" item="propertyName" index="index">
              and  ${propertyName}=#{values.[${index}]}
            </foreach>
    </select>

    <select id="countByProperties" resultType="java.lang.Integer">
        select count(1) from sys_organization
            where is_deleted=0
            <foreach collection="propertyNames" item="propertyName" index="index">
              and ${propertyName}=#{values.[${index}]}
            </foreach>
    </select>

    <select id="mapByProperties" resultType="com.iqc.system.dbo.SysOrganizationDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="propertyNames" item="propertyName" index="index">
              and  ${propertyName}=#{values.[${index}]}
            </foreach>
    </select>

    <update id="updateNotNullFieldsByProperties">
        update sys_organization set
                <if test="updateDO.id != null">id=#{updateDO.id},</if>
                <if test="updateDO.name != null">name=#{updateDO.name},</if>
                <if test="updateDO.description != null">description=#{updateDO.description},</if>
                <if test="updateDO.pid != null">pid=#{updateDO.pid},</if>
                <if test="updateDO.path != null">path=#{updateDO.path},</if>
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0
         <foreach collection="propertyNames" item="propertyName" index="index">
           and  ${propertyName}=#{values.[${index}]}
         </foreach>
    </update>

    <update id="updateAllFieldsByProperties">
        update sys_organization set
                id=#{updateDO.id}
                name=#{updateDO.name}
                description=#{updateDO.description}
                pid=#{updateDO.pid}
                path=#{updateDO.path}
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0
         <foreach collection="propertyNames" item="propertyName" index="index">
           and  ${propertyName}=#{values.[${index}]}
         </foreach>
    </update>

    <update id="deleteByProperties">
        update sys_organization set
                is_deleted=1,
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0
         <foreach collection="propertyNames" item="propertyName" index="index">
           and  ${propertyName}=#{values.[${index}]}
         </foreach>
    </update>

    <select id="_listByQueryItems" resultType="com.iqc.system.dbo.SysOrganizationDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </select>

    <select id="_mapByQueryItems" resultType="com.iqc.system.dbo.SysOrganizationDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </select>

    <select id="_countByQueryItems" resultType="java.lang.Integer">
        select count(1) from sys_organization
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </select>

   <update id="_updateNotNullFieldsByQueryItems">
         update sys_organization set
                <if test="updateDO.id != null">id=#{updateDO.id},</if>
                <if test="updateDO.name != null">name=#{updateDO.name},</if>
                <if test="updateDO.description != null">description=#{updateDO.description},</if>
                <if test="updateDO.pid != null">pid=#{updateDO.pid},</if>
                <if test="updateDO.path != null">path=#{updateDO.path},</if>
                gmt_modified=CURRENT_TIMESTAMP
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </update>

    <update id="_updateNotAllFieldsByQueryItems">
         update sys_organization set
                id=#{updateDO.id}
                name=#{updateDO.name}
                description=#{updateDO.description}
                pid=#{updateDO.pid}
                path=#{updateDO.path}
                gmt_modified=CURRENT_TIMESTAMP
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </update>

    <update id="_deleteByQueryItems">
         update sys_organization set
                is_deleted=1,
                gmt_modified=CURRENT_TIMESTAMP
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </update>

    <select id="_listByPageQuery" resultType="com.iqc.system.dbo.SysOrganizationDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                         #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
            <if test="pageQuery.sortFieldNameForDatabase!=null">
                order by ${pageQuery.sortFieldNameForDatabase} ${pageQuery.sortType}
            </if>
            limit #{values.pageOffset},#{pageQuery.pageSize}
    </select>


    <select id="_countByPageQuery" resultType="java.lang.Integer">
        select count(1) from sys_organization
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                    <choose>
                        <when test="item.type.optr=='in' || item.type.optr=='not in'">
                            <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                                #{inItem}
                            </foreach>
                        </when>
                        <otherwise>
                           #{values.[${item.valueKeyName}]}
                        </otherwise>
                    </choose>
                </if>
            </foreach>
    </select>



          <select id="listById" resultType="com.iqc.system.dbo.SysOrganizationDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and id=#{id}
          </select>

          <select id="countById" resultType="java.lang.Integer">
                select count(1) from sys_organization
                where is_deleted=0 and id=#{id}
           </select>

           <select id="mapById" resultType="com.iqc.system.dbo.SysOrganizationDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and id=#{id}
           </select>

           <update id="updateNotNullFieldsById">
              update sys_organization set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.pid != null">pid=#{updateDO.pid},</if>
                      <if test="updateDO.path != null">path=#{updateDO.path},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and id=#{id}
          </update>

          <update id="updateAllFieldsById">
                update sys_organization set
                        id=#{updateDO.id},
                        name=#{updateDO.name},
                        description=#{updateDO.description},
                        pid=#{updateDO.pid},
                        path=#{updateDO.path},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and id=#{id}
            </update>




            <!--property in -->

         <select id="listByInId" resultType="com.iqc.system.dbo.SysOrganizationDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and id in
                  <foreach collection="idList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInId" resultType="java.lang.Integer">
                select count(1) from sys_organization
                where is_deleted=0 and id in
                      <foreach collection="idList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInId" resultType="com.iqc.system.dbo.SysOrganizationDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and id in
                        <foreach collection="idList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInId">
              update sys_organization set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.pid != null">pid=#{updateDO.pid},</if>
                      <if test="updateDO.path != null">path=#{updateDO.path},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and id in
                       <foreach collection="idList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInId">
                update sys_organization set
                        id=#{updateDO.id},
                        name=#{updateDO.name},
                        description=#{updateDO.description},
                        pid=#{updateDO.pid},
                        path=#{updateDO.path},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and id in
                       <foreach collection="idList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInId">
                update sys_organization set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and id in
                        <foreach collection="idList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByName" resultType="com.iqc.system.dbo.SysOrganizationDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and name=#{name}
          </select>

          <select id="countByName" resultType="java.lang.Integer">
                select count(1) from sys_organization
                where is_deleted=0 and name=#{name}
           </select>

           <select id="mapByName" resultType="com.iqc.system.dbo.SysOrganizationDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and name=#{name}
           </select>

           <update id="updateNotNullFieldsByName">
              update sys_organization set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.pid != null">pid=#{updateDO.pid},</if>
                      <if test="updateDO.path != null">path=#{updateDO.path},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and name=#{name}
          </update>

          <update id="updateAllFieldsByName">
                update sys_organization set
                        id=#{updateDO.id},
                        name=#{updateDO.name},
                        description=#{updateDO.description},
                        pid=#{updateDO.pid},
                        path=#{updateDO.path},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and name=#{name}
            </update>


            <update id="deleteByName">
                update sys_organization set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and name=#{name}
            </update>



            <!--property in -->

         <select id="listByInName" resultType="com.iqc.system.dbo.SysOrganizationDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and name in
                  <foreach collection="nameList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInName" resultType="java.lang.Integer">
                select count(1) from sys_organization
                where is_deleted=0 and name in
                      <foreach collection="nameList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInName" resultType="com.iqc.system.dbo.SysOrganizationDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and name in
                        <foreach collection="nameList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInName">
              update sys_organization set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.pid != null">pid=#{updateDO.pid},</if>
                      <if test="updateDO.path != null">path=#{updateDO.path},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and name in
                       <foreach collection="nameList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInName">
                update sys_organization set
                        id=#{updateDO.id},
                        name=#{updateDO.name},
                        description=#{updateDO.description},
                        pid=#{updateDO.pid},
                        path=#{updateDO.path},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and name in
                       <foreach collection="nameList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInName">
                update sys_organization set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and name in
                        <foreach collection="nameList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByDescription" resultType="com.iqc.system.dbo.SysOrganizationDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and description=#{description}
          </select>

          <select id="countByDescription" resultType="java.lang.Integer">
                select count(1) from sys_organization
                where is_deleted=0 and description=#{description}
           </select>

           <select id="mapByDescription" resultType="com.iqc.system.dbo.SysOrganizationDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and description=#{description}
           </select>

           <update id="updateNotNullFieldsByDescription">
              update sys_organization set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.pid != null">pid=#{updateDO.pid},</if>
                      <if test="updateDO.path != null">path=#{updateDO.path},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and description=#{description}
          </update>

          <update id="updateAllFieldsByDescription">
                update sys_organization set
                        id=#{updateDO.id},
                        name=#{updateDO.name},
                        description=#{updateDO.description},
                        pid=#{updateDO.pid},
                        path=#{updateDO.path},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and description=#{description}
            </update>


            <update id="deleteByDescription">
                update sys_organization set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and description=#{description}
            </update>



            <!--property in -->

         <select id="listByInDescription" resultType="com.iqc.system.dbo.SysOrganizationDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and description in
                  <foreach collection="descriptionList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInDescription" resultType="java.lang.Integer">
                select count(1) from sys_organization
                where is_deleted=0 and description in
                      <foreach collection="descriptionList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInDescription" resultType="com.iqc.system.dbo.SysOrganizationDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and description in
                        <foreach collection="descriptionList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInDescription">
              update sys_organization set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.pid != null">pid=#{updateDO.pid},</if>
                      <if test="updateDO.path != null">path=#{updateDO.path},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and description in
                       <foreach collection="descriptionList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInDescription">
                update sys_organization set
                        id=#{updateDO.id},
                        name=#{updateDO.name},
                        description=#{updateDO.description},
                        pid=#{updateDO.pid},
                        path=#{updateDO.path},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and description in
                       <foreach collection="descriptionList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInDescription">
                update sys_organization set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and description in
                        <foreach collection="descriptionList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByPid" resultType="com.iqc.system.dbo.SysOrganizationDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and pid=#{pid}
          </select>

          <select id="countByPid" resultType="java.lang.Integer">
                select count(1) from sys_organization
                where is_deleted=0 and pid=#{pid}
           </select>

           <select id="mapByPid" resultType="com.iqc.system.dbo.SysOrganizationDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and pid=#{pid}
           </select>

           <update id="updateNotNullFieldsByPid">
              update sys_organization set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.pid != null">pid=#{updateDO.pid},</if>
                      <if test="updateDO.path != null">path=#{updateDO.path},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and pid=#{pid}
          </update>

          <update id="updateAllFieldsByPid">
                update sys_organization set
                        id=#{updateDO.id},
                        name=#{updateDO.name},
                        description=#{updateDO.description},
                        pid=#{updateDO.pid},
                        path=#{updateDO.path},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and pid=#{pid}
            </update>


            <update id="deleteByPid">
                update sys_organization set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and pid=#{pid}
            </update>



            <!--property in -->

         <select id="listByInPid" resultType="com.iqc.system.dbo.SysOrganizationDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and pid in
                  <foreach collection="pidList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInPid" resultType="java.lang.Integer">
                select count(1) from sys_organization
                where is_deleted=0 and pid in
                      <foreach collection="pidList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInPid" resultType="com.iqc.system.dbo.SysOrganizationDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and pid in
                        <foreach collection="pidList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInPid">
              update sys_organization set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.pid != null">pid=#{updateDO.pid},</if>
                      <if test="updateDO.path != null">path=#{updateDO.path},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and pid in
                       <foreach collection="pidList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInPid">
                update sys_organization set
                        id=#{updateDO.id},
                        name=#{updateDO.name},
                        description=#{updateDO.description},
                        pid=#{updateDO.pid},
                        path=#{updateDO.path},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and pid in
                       <foreach collection="pidList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInPid">
                update sys_organization set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and pid in
                        <foreach collection="pidList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByPath" resultType="com.iqc.system.dbo.SysOrganizationDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and path=#{path}
          </select>

          <select id="countByPath" resultType="java.lang.Integer">
                select count(1) from sys_organization
                where is_deleted=0 and path=#{path}
           </select>

           <select id="mapByPath" resultType="com.iqc.system.dbo.SysOrganizationDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and path=#{path}
           </select>

           <update id="updateNotNullFieldsByPath">
              update sys_organization set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.pid != null">pid=#{updateDO.pid},</if>
                      <if test="updateDO.path != null">path=#{updateDO.path},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and path=#{path}
          </update>

          <update id="updateAllFieldsByPath">
                update sys_organization set
                        id=#{updateDO.id},
                        name=#{updateDO.name},
                        description=#{updateDO.description},
                        pid=#{updateDO.pid},
                        path=#{updateDO.path},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and path=#{path}
            </update>


            <update id="deleteByPath">
                update sys_organization set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and path=#{path}
            </update>



            <!--property in -->

         <select id="listByInPath" resultType="com.iqc.system.dbo.SysOrganizationDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and path in
                  <foreach collection="pathList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInPath" resultType="java.lang.Integer">
                select count(1) from sys_organization
                where is_deleted=0 and path in
                      <foreach collection="pathList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInPath" resultType="com.iqc.system.dbo.SysOrganizationDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and path in
                        <foreach collection="pathList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInPath">
              update sys_organization set
                      <if test="updateDO.id != null">id=#{updateDO.id},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.pid != null">pid=#{updateDO.pid},</if>
                      <if test="updateDO.path != null">path=#{updateDO.path},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and path in
                       <foreach collection="pathList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInPath">
                update sys_organization set
                        id=#{updateDO.id},
                        name=#{updateDO.name},
                        description=#{updateDO.description},
                        pid=#{updateDO.pid},
                        path=#{updateDO.path},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and path in
                       <foreach collection="pathList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInPath">
                update sys_organization set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and path in
                        <foreach collection="pathList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>

</mapper>

